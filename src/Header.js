import React from "react";
import "./Header.scss";

const Header = () => {
  return (
    <header className="myheader">
      <p className="myheader__text">This is the header!</p>
    </header>
  );
};

export default Header;
